# Local runtime data (contains account emails) — regenerated on run
.usage-cache.json

# Python local environments and caches
.venv/
__pycache__/
*.pyc

# Local logs
*.log

# Usage-tracker runtime data (contains account emails) — regenerated on run
usage-history.jsonl
rows.json
ClaudeUsageTracker/backups/

# DuchPayWeb local config (contains editPassword) — copy from settings.sample.json
DuchPayWeb/settings.json
